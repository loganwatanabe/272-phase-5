
<h2> Students who have not signed their waivers</h2>
<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Age</th>
    <th>Rank</th>
  </tr>

  <% @unsigned.each do |student| %>
    <tr>
      <td><%= link_to student.name, student_path(student) %></td>
      <td><%= student.age %></td>
      <td><%= "#{rank_name(student.rank)}" %></td>
    </tr>
  <% end %>
</table>
<span class="smtext">
<%= will_paginate @unsigned, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>



<!--This is to ensure that pages with no content don't look bad, with the footer being all the way near the top -->
<% if @unsigned.size == 0 %>
<h5>No students need to sign waivers</h5>
<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>
<% elsif @unsigned.size<20 ; (20-@unsigned.size).times do %>
	<br>
<% end; end %>