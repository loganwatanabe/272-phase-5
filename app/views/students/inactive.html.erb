<h2>Inactive Students</h2>
<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Age</th>
    <th>Rank</th>
  </tr>

  <% @inactive_students.each do |student| %>
    <tr>
      <td><%= link_to student.name, student_path(student) %></td>
      <td><%= student.age %></td>
      <td><%= "#{rank_name(student.rank)}" %></td>
    </tr>
  <% end %>
</table>
<span class="smtext">
<%= will_paginate @inactive_students, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>


<!--This is to ensure that pages with no content don't look bad, with the footer being all the way near the top -->
<% if @inactive_students.size == 0 %>
<h5>No inactive students</h5>
<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>
<% elsif @inactive_students.size<20 ; (20-@inactive_students.size).times do %>
  <br>
<% end; end %>
